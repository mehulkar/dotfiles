#!/usr/bin/env bash

command -v starship &>/dev/null && eval "$(starship init zsh)"
# Note: ~/.env is sourced from ~/.zshenv so non-interactive shells get it too.
source $HOME/.pythonstuff
source $HOME/.path
source $HOME/.aliases

if [ -f "$HOME/.secret" ]; then
  source "$HOME/.secret"
fi
